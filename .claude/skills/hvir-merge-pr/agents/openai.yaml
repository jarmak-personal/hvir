interface:
  display_name: 'hvir Merge PR'
  short_description: 'Accept one exact final hvir PR'
  default_prompt: 'Use $hvir-merge-pr to merge and reconcile one explicitly authorized final hvir pull-request candidate.'
policy:
  allow_implicit_invocation: false
