interface:
  display_name: 'hvir Merge PR'
  short_description: 'Accept one exact ordinary hvir candidate'
  default_prompt: 'Use $hvir-merge-pr to merge and reconcile this explicitly authorized ordinary hvir pull request.'
policy:
  allow_implicit_invocation: false
